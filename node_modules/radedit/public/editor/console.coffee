socket.on 'radedit:log', (lines) ->
	forEach lines, (line) ->
		className = line[0]
		innerText = line[1]
		line = addElement 'console', 'pre'
		setClass line, className
		setText line, innerText
