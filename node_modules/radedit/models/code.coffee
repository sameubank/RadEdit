db.define 'file',
	rel: String
	content: String
	modified: Number

db.define 'tree',
	string: String

db.model 'tree', (model) ->
	return
	model.one (err, tree) ->
		if tree
			fs.treeString = tree.string
			log 
		else
			model.create
				id: 1
				string: ''
